---
layout: base.njk
title: Certified Devices
permalink: /certified-products/
---

<div class="wrapper wrapper-a">
  <div class="devices-page">
    <div class="devices-header">
      <h1>Certified Works with Home Assistant Products</h1>
      <p class="contained">
        Here is a running list of the certified devices in the Works with Home
        Assistant program. We are missing an up-to-date list from a few of our
        partners as they expand their product lines and testing takes place, but
        these will be added over the coming weeks.
      </p>
      <p class="contained">
        As always, this is not a commercial project for us and your privacy is
        paramount. These links are not affiliate links and we do not earn
        anything from them. This list simply exists to group all items in a
        searchable format so you don’t have to look at individual launch blogs
        or integration pages to see up-to-date devices.
      </p>
      <p class="contained">
        We’ve included relevant information about which functions are available
        from within Home Assistant, and known limitations: for example, Home
        Assistant and Matter, don’t currently support smart lock calibration, so
        there’s information on how this can be done outside of Home Assistant,
        as it’s generally a function you’d only use once during set-up. This
        information is included to help you make better informed choices, but as
        always, make sure whatever you’re purchasing for your smart home works
        for you.
      </p>
    </div>

    <div class="devices-filters">
      <input
        type="text"
        id="searchInput"
        placeholder="Search by brand, device name, model number..."
      />
      <select id="brandFilter">
        <option value="">All Brands</option>
        {% for filename, data in collections.all[0].data.devices %}
        <option value="{{ data.companyName }}">
          {{ data.companyName }}
        </option>
        {% endfor %}
      </select>
      <select id="protocolFilter">
        <option value="">All Protocols</option>
      </select>
      <select id="deviceTypeFilter">
        <option value="">All Device Types</option>
      </select>
      <select id="regionFilter">
        <option value="">All Regions</option>
      </select>
    </div>

    <div class="table-container">
      <table id="devicesTable" class="devices-table">
        <thead>
          <tr>
            <th data-sort="brand" class="sortable">
              Brand <span class="sort-icon"></span>
            </th>
            <th data-sort="deviceName" class="sortable">
              Device Name <span class="sort-icon"></span>
            </th>
            <th data-sort="modelNumber" class="sortable">
              Model Number <span class="sort-icon"></span>
            </th>
            <th data-sort="protocol" class="sortable">
              Certified Protocol <span class="sort-icon"></span>
            </th>
            <th data-sort="deviceType" class="sortable">
              Device Type <span class="sort-icon"></span>
            </th>
            <th data-sort="region">Region</th>
            <th>Related Products</th>
            <th class="remark-header" title="Functionality Remark">Note</th>
            <th>Link</th>
          </tr>
        </thead>
        <tbody id="devicesTableBody">
          {% for filename, data in collections.all[0].data.devices %} {% for
          device in data.devices %}
          <tr
            data-brand="{{ data.companyName }}"
            data-protocol="{{ device.protocol }}"
            data-device-type="{{ device.deviceType }}"
            data-regions="{{ device.region | join(',') }}"
            data-search="{{ (data.companyName + ' ' + device.deviceName + ' ' + device.modelNumber + ' ' + device.deviceType) | lower }}"
          >
            <td>{{ data.companyName }}</td>
            <td>{{ device.deviceName }}</td>
            <td>{{ device.modelNumber }}</td>
            <td>{{ device.protocol }}</td>
            <td>{{ device.deviceType }}</td>
            <td class="region-cell">
              {% for region in device.region %}
              <span class="region-badge">{{ region }}</span>
              {% endfor %}
            </td>
            <td>
              {% if device.relatedProducts.url %}
              <a
                href="{{ device.relatedProducts.url }}"
                target="_blank"
                rel="noopener noreferrer"
                >{{ device.relatedProducts.text }}</a
              >
              {% elif device.relatedProducts.text %}
              {{ device.relatedProducts.text }}
              {% endif %}
            </td>
            <td class="remark-cell">
              {% if device.functionalityRemark %}
              <button
                class="remark-icon"
                aria-label="View note"
                data-remark="{{ device.functionalityRemark }}"
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  height="24px"
                  viewBox="0 -960 960 960"
                  width="24px"
                  fill="currentColor"
                >
                  <path
                    d="M440-280h80v-240h-80v240Zm40-320q17 0 28.5-11.5T520-640q0-17-11.5-28.5T480-680q-17 0-28.5 11.5T440-640q0 17 11.5 28.5T480-600Zm0 520q-83 0-156-31.5T197-197q-54-54-85.5-127T80-480q0-83 31.5-156T197-763q54-54 127-85.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 83-31.5 156T763-197q-54 54-127 85.5T480-80Zm0-80q134 0 227-93t93-227q0-134-93-227t-227-93q-134 0-227 93t-93 227q0 134 93 227t227 93Zm0-320Z"
                  />
                </svg>
                <span class="remark-tooltip">{{
                  device.functionalityRemark
                }}</span>
              </button>
              {% endif %}
            </td>
            <td>
              {% if device.websiteLink %}
              <a
                href="{{ device.websiteLink }}"
                target="_blank"
                rel="noopener noreferrer"
                >View</a
              >
              {% endif %}
            </td>
          </tr>
          {% endfor %} {% endfor %}
        </tbody>
      </table>
    </div>

    <div class="pagination-wrapper">
      <div class="devices-count">
        Showing <span id="visibleCount">0</span> of
        <span id="filteredCount">0</span> devices (<span id="totalCount"
          >0</span
        >
        total)
      </div>

      <div class="pagination-controls">
        <button id="prevPage" class="pagination-btn" disabled>
          <svg
            width="16"
            height="16"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="15 18 9 12 15 6"></polyline>
          </svg>
          Previous
        </button>

        <div id="pageNumbers" class="page-numbers"></div>

        <span id="pageInfo" class="page-info">Page 1 of 1</span>

        <button id="nextPage" class="pagination-btn">
          Next
          <svg
            width="16"
            height="16"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="9 18 15 12 9 6"></polyline>
          </svg>
        </button>
      </div>

      <div class="items-per-page">
        <label for="itemsPerPage">Show:</label>
        <select id="itemsPerPage">
          <option value="10">10</option>
          <option value="25" selected>25</option>
          <option value="50">50</option>
          <option value="100">100</option>
        </select>
        <span>per page</span>
      </div>
    </div>
  </div>
</div>

<script src="/js/devices-table.js"></script>
